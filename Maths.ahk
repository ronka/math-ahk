; ## Math Operators

; print lim symbol
!+l::
Send, lim┬(n→∞)
return

; print floor value
!+f::
Send, ⌊x⌋
return

; print sigma
!+s::
Send, ∑
return

; print big right arrow
!+.::
Send, ⇒
return

; print big left arrow
!+,::
Send, ⇐
return

; print double line equation
!+d::
Send, █(@)
return

; print bullet(multiple) operator
!+8::
Send, ∙
return

; print radical sign
!+q::
Send, \sqrt(
return

; print squre on X
!+w::
Send, \sqrt(n&x)
return

; print cap
!+h::
Send, ∩
return

; print cup
!+u::
Send, ∪
return

; ## Math Symbols

; print Epsilon
!+e::
Send, ε
return

; print infinity
!+i::
Send, ∞
return

; print alpha
!+a::
Send, α
return

; print beta
!+b::
Send, β
return